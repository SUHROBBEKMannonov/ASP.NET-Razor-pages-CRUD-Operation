@page
@model RazorCRUDProjectWeb.Pages.ModelUserCVTemp.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ModelUsers[0].FirstName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ModelUsers[0].LastName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ModelUsers[0].Email)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ModelUsers[0].PhoneNumber)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ModelUsers[0].BirthOfDate)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.ModelUsers) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FirstName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LastName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Email)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PhoneNumber)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BirthOfDate)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
